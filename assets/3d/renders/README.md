# Generated GLB logo images

Files in this directory are generated from the sibling `assets/3d/*.glb` sources by:

```sh
npm run logos:render
```

The renderer produces transparent square PNG and WebP images and records exact source/output hashes in `manifest.json`. Do not hand-edit generated files.
